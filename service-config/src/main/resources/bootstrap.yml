spring:
  application:
    name: configserver
  cloud:
    config:
      enabled: false
  profiles:
    active: native
logging:
  file: config.log
encrypt:
  failOnError: false
  keyStore:
    location: classpath:key_store.jks
    password: ${KEY_STORE_PASSWORD:devops}
    alias: devops
server:
  port: 8888
