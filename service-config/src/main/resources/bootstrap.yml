spring:
  application:
    name: configserver
  cloud:
    config:
      enabled: false
  profiles:
    active: native
  jmx:
    enabled: true
#encrypt:
#  failOnError: false
#  keyStore:
#    location: classpath:keystore.jks
#    password: ${KEYSTORE_PASSWORD:foobar} # don't use a default in production
#    alias: test
server:
  port: 8888
management:
  context-path: /admin
