spring:
  profiles:
    active: dev
  application:
    name: monitor
  cloud:
    config:
      uri: https://localhost:8888/config
      failFast: true

authserver.CustomSSLSocketFactoryClassName: com.netflix.http4.ssl.AcceptAllSocketFactory